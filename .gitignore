# Repositório de referência (projeto Lovable autônomo) — NÃO versionar
/Referencias/CloneLovable/alcateia-booking/

# Ambiente / segredos (nunca commitar)
.env
.env.*
!.env.example

# dependências e builds
node_modules/
dist/
build/
out/
public/
*.tsbuildinfo

# arquivo exportado do projeto (não versionar)
*.zip

# Supabase CLI cache local (não versionar)
**/supabase/.temp/

# sistema / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp